apiVersion: v1
kind: Service
metadata:
  name: {{ include "whiskymap.fullname" . }}
  labels:
    {{- include "whiskymap.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "whiskymap.selectorLabels" . | nindent 4 }}
